QuickXml Writer lets you get Trades from dukascopy strategy to XML.


To actually use it you must just paste in your strategies :

//Variables used by XmlWriter.
String FileLocationForXml ="c:\\Trading\\Trades1.Xml";
public static Document dom;
public static String SystemName="WINPROF";
public static String MagicNumber = "121266";


Inside On Message add also :


 public void onMessage(IMessage message) throws JFException 
    {
         if (message != null && message.getType() == IMessage.Type.ORDER_CLOSE_OK) {
                IOrder lastOne = message.getOrder();
                double profitsLoss = lastOne.getProfitLossInPips();
                if (profitsLoss > 0) {
                    wins++;
                } else {
                    losses++;
                }
                int t = wins + losses;
                console.getOut().println("Super Order: " + lastOne.getLabel() +  " " + lastOne.getOrderCommand() + " Pips: " + profitsLoss + " Loss: " + losses + "(" + (losses * 100 / t) + "%) Win: " + wins + "(" + (wins * 100 / t) + "%)");
           
             
                
                  ParseMessageToTradeXML(message,"Close"); }
            
              if (message != null && message.getType() == IMessage.Type.ORDER_FILL_OK && message.getType() != IMessage.Type.ORDER_CLOSE_OK) {
                  PrintaMessage(message.getContent());
                  ParseMessageToTradeXML(message,"Open"); }
    }


This will make the XmlWriter class write xml (which can be used for other things like web services, MT4 cloner's, analysis and so forth).

<?xml version="1.0" encoding="UTF-8"?>
<Trade>
    <Symbol>EUR/USD</Symbol>
    <Operation>Close</Operation>
    <System>WINPROF</System>
    <EntryPrice>1.31271</EntryPrice>
    <StopPrice>1.30611</StopPrice>
    <LimitPrice>1.31491</LimitPrice>
    <Side>Buy</Side>
    <OrderType>Market</OrderType>
    <TrailingStop>1</TrailingStop>
    <TimeZone>0</TimeZone>
    <DayOfYear>6</DayOfYear>
    <DayOfWeek>7</DayOfWeek>
    <Multiplier>1</Multiplier>
    <Version>1.3</Version>
</Trade>
